# fly.toml app configuration file generated for crossfit-reservas-mvp on 2025-08-10T23:16:21Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'crossfit-reservas-mvp'
primary_region = 'gru'

[build]

[http_service]
  auto_stop_machines = "stop"   # se apaga sola sin tráfico
  auto_start_machines = true    # cualquier request (el PING) la enciende
  min_machines_running = 0
  internal_port = 8001
  force_https = true
  processes = ["app"]

[env]
  TZ = "America/Santiago"

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
